# Development dependencies. Unlike requirements.txt, these are not published as
# package metadata, so they are bounded tightly to keep local and CI runs
# reproducible. Lower bounds are the versions currently tested against.
build>=1.2.2,<2.0

pytest>=8.3.5,<9.0
pytest-cov>=7.1.0,<8.0
mock>=5.2.0,<6.0

# Bounded to a single minor release: pylint introduces new checks in minor
# versions (R0917 arrived in 3.3, for example), which turns 'make lint' red
# without any code change. Bump this deliberately, not incidentally.
pylint>=3.3.7,<3.4
