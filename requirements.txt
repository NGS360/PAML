# Runtime dependencies. These are published as the package's install_requires
# via pyproject.toml, so use lower bounds (the version we test against) plus an
# upper bound at the next incompatible release - never exact '==' pins, which
# would force version conflicts on anything that depends on cwl_platform.
chardet>=5.2.0,<6.0
smart_open>=7.1.0,<8.0
# arvados-python-client tracks the Arvados API version and can break between
# minor releases, so it is bounded more tightly than the others.
arvados-python-client>=3.2.1,<3.3.0
sevenbridges-python>=2.11.2,<3.0
